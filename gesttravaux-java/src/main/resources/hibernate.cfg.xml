<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/gesttravaux_db?useSSL=false&amp;serverTimezone=UTC</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">password</property>

        <!-- JDBC connection pool settings -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <!-- Use "update" for production, "create-drop" for development -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Disable the second-level cache -->
        <property name="hibernate.cache.use_second_level_cache">false</property>

        <!-- Mapped entity classes -->
        <mapping class="com.immosync.gesttravaux.models.Entrepreneur"/>
        <mapping class="com.immosync.gesttravaux.models.Categorie"/>
        <mapping class="com.immosync.gesttravaux.models.Proprietaire"/>
        <mapping class="com.immosync.gesttravaux.models.Bien"/>
        <mapping class="com.immosync.gesttravaux.models.Prestation"/>
        <mapping class="com.immosync.gesttravaux.models.Inspecteur"/>
        <mapping class="com.immosync.gesttravaux.models.Chantier"/>
        <mapping class="com.immosync.gesttravaux.models.Devis"/>
        <mapping class="com.immosync.gesttravaux.models.Document"/>
        <mapping class="com.immosync.gesttravaux.models.Administrateur"/>
        <mapping class="com.immosync.gesttravaux.models.AppelOffre"/>
    </session-factory>
</hibernate-configuration>
